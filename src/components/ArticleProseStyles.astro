<style is:global>
  .article-content h2 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-primary);
    margin-top: 2rem;
    margin-bottom: 1rem;
  }

  @media (min-width: 640px) {
    .article-content h2 {
      font-size: 1.875rem;
    }
  }

  .article-content > h2:first-child {
    margin-top: 0;
  }

  .article-content h3 {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--color-primary);
    margin-top: 1.75rem;
    margin-bottom: 0.75rem;
  }

  .article-content p {
    margin-bottom: 1.25rem;
    line-height: 1.8;
  }

  .article-content ul {
    margin-bottom: 1.5rem;
    padding-left: 0;
  }

  .article-content li {
    margin-bottom: 0.75rem;
    line-height: 1.7;
    padding-left: 1.75rem;
    position: relative;
    list-style: none;
  }

  .article-content ul li:before {
    content: "●";
    color: var(--color-light-blue);
    font-size: 0.6em;
    position: absolute;
    left: 0;
    top: 0.45em;
  }

  .article-content strong {
    font-weight: 700;
    color: var(--color-primary);
  }

  /* Example dialogues — calm blue, not warning pink */
  .article-content blockquote {
    padding: 1rem 1.5rem 0.9rem 1.5rem;
    border-radius: 0.5rem;
    margin: 2rem 0;
    font-size: 1.05rem;
    font-weight: 500;
    line-height: 1.6;
    border-left: 4px solid var(--color-light-blue);
    background: linear-gradient(
      135deg,
      color-mix(in srgb, var(--color-light-blue) 14%, white) 0%,
      color-mix(in srgb, var(--color-primary) 8%, white) 100%
    );
    color: var(--color-primary);
  }

  .article-content blockquote p {
    margin-bottom: 1rem;
    color: inherit;
  }

  .article-content blockquote *:last-child {
    margin-bottom: 0;
  }

  .article-content blockquote strong {
    font-weight: 700;
    color: #1a8ab5;
  }

  .article-content blockquote.course-congrats {
    margin-top: 2.5rem;
    border-left-color: #27ae60;
    background: linear-gradient(
      135deg,
      color-mix(in srgb, #27ae60 16%, white) 0%,
      color-mix(in srgb, var(--color-light-blue) 12%, white) 100%
    );
    padding: 1.5rem 1.75rem 1.25rem;
  }

  .article-content blockquote.course-congrats > p:first-child {
    font-size: 1.75rem;
    font-weight: 700;
    line-height: 1.3;
    margin-bottom: 1.25rem;
    color: var(--color-primary);
  }

  @media (min-width: 640px) {
    .article-content blockquote.course-congrats > p:first-child {
      font-size: 2rem;
    }
  }
</style>
